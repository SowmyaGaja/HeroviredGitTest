Git Hub
1 # Open Git Hub Bash

2# Then check user name pswd using code
git config --list

3# git init
this will initialise the repo and we get .git file created

4# git status 
status of current files
red files indicate commit is not done.

5# Move the changes to Staging area - Modification
git add Sample.txt

to check if file is added
git status

6# We commit it online or staging area.
use the comment while committing to indicate what changes are done.

git commit -m 

7 collaborators - used for giving access to the git hub

8 cd space .. - > moves outside folder
cd space foldername enters the folder

9 Configuration to connect with web git.
git remote add origin <your repository url>

10 view current setting to verify
git remote -v


